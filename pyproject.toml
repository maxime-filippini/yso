[project]
name = "yso"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["click>=8.1.8", "fastapi[standard]>=0.122.0", "yso"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
yso = { workspace = true }

[project.scripts]
yso = "yso.cli.cli:cli"
